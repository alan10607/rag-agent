[pytest]
# Base configuration
testpaths = tests
norecursedirs = tests/manual venv .git

# Register custom markers (Fixes the "UnknownMarkWarning")
markers =
    manual: tests that require manual intervention.
    integration: tests that interact with external services (like Qdrant).
